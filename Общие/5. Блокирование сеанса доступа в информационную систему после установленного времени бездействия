########## Настраиваем автоматическую блокировку systemd-logind (15 минут) ##########
sudo nano /etc/systemd/logind.conf
# Изменить
# IdleAction=lock
# IdleActionSec=15min
sudo systemctl restart systemd-logind

########## Настраиваем автоматическую блокировку для всех SSH-сеансов (tmux, 15 минут) ##########
sudo apt install tmux vlock
sudo nano ~/.bashrc
# В файле в конце добавляем для автозапуска tmux при ssh
# if [[ -n "$SSH_CONNECTION" ]] && [[ -z "$TMUX" ]]; then
#    tmux attach-session -t ssh || tmux new-session -s ssh
# fi
source ~/.bashrc
sudo nano ~/.tmux.conf
# В файле
# set -g lock-after-time 900
# set -g lock-command vlock
